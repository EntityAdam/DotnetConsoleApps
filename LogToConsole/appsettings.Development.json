{
  "AZURE_KEYVAULT_URI": "https://eaconsoleappsdemodevkv.vault.azure.net/",
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    },
    "Console": {
      "LogLevel": {
        "Default": "Information", //change this to trace and run again
        "Microsoft": "Information" //change this to warning and run again
      }
    }
  }
}